// The child's own goals — the screen that used to BE the goals tab. The tab
// now shows Maqsaddoshlar (goal-mates); this is where its "Qo'shish" story
// lands, because adding a goal is what creates new matches.
export { default } from '@/screens/goals/goals-screen';
