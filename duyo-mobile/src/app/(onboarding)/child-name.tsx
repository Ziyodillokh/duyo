import { Text, View } from 'react-native';

// Stub — real ChildName + AgeSelection screen lands in Phase 1.5.
export default function ChildNameScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-2xl font-bold text-foreground">Isming nima?</Text>
      <Text className="text-sm text-muted-foreground mt-2">Phase 1.5 — soon</Text>
    </View>
  );
}
