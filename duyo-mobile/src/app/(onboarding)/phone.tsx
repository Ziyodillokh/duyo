import { Text, View } from 'react-native';

// Stub — real PhoneAuth + OTP screens land in Phase 1.3.
export default function PhoneScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-2xl font-bold text-foreground">Telefon raqamingiz</Text>
      <Text className="text-sm text-muted-foreground mt-2">Phase 1.3 — soon</Text>
    </View>
  );
}
